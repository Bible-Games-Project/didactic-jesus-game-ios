import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.biblegamesproject.didacticjesusgame.pro",
  appName: "Didactic Jesus Game (iOS)",
  webDir: "dist",
};

export default config;
